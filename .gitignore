# 数据集（2025 张猫狗图片，体积大，不纳入版本控制）
# 注意：这里写 data/* 而不是 data/ —— git 的规则是「父目录一旦被整体忽略，
# 其子文件无法再用 ! 恢复」，写成 data/ 会让下面的 !data/README.md 失效。
data/*
!data/README.md

# 训练产物：模型权重
*.pth
best_model.pth

# Python 缓存
__pycache__/

# IDE / 工具
.idea/
.vscode/
.codebuddy/
