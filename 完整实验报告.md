# GoogLeNet 猫狗图像分类实验报告

**课程/项目**：基于 GoogLeNet 的猫狗二分类对比实验
**实验内容**：从零手写 GoogLeNet 训练 vs. ImageNet 预训练迁移学习
**提交日期**：2026-09

---

## 摘要

本实验在猫狗二分类任务上，对比了**从零手写 GoogLeNet**与 **ImageNet 预训练迁移学习**两条技术路线。从零版经历数据增强、正则化调整、Batch Normalization 引入等五轮优化迭代，最终在测试集（共 304 张）上达到 **82.24%** 准确率；迁移学习版复用预训练特征并微调，准确率达 **97.04%**。结果表明：在小规模数据（训练 1721 张 / 测试 304 张）下，迁移学习显著优于从零训练。需要说明的是，从零版在本次的训练预算下（`batch_size=128`、每 epoch 仅 11 个梯度步）**远未收敛**，因此本报告不主张"82% 是数据规模上限"——更诚实的表述是"训练预算不足"与"初期训练不稳定"两个候选原因尚未分离，详见第 5.5 节「已知限制」。

**关键词**：图像分类；GoogLeNet；迁移学习；数据增强；Batch Normalization；过拟合

---

## 1 引言

### 1.1 实验背景

猫狗二分类是图像分类领域最具代表性的入门基准任务。本实验选择 GoogLeNet 作为主干网络，原因有二：一是其 Inception 多尺度结构在参数量可控的前提下具备较强表达能力；二是通过手写实现可深入理解其模块设计与训练技巧。

### 1.2 实验目标

1. 从零实现 GoogLeNet 并完成训练，探究小数据场景下的训练稳定性与优化策略；
2. 引入 ImageNet 预训练权重进行迁移学习，与从零训练做对照；
3. 通过量化指标与可视化手段，分析两条路线的性能差异及其成因。

---

## 2 数据集与实验环境

### 2.1 数据集

本实验数据取自 Kaggle 公开数据集 **Cat and Dog**（<https://www.kaggle.com/datasets/tongpython/cat-and-dog>，Kaggle 页面标注许可证为 **CC0: Public Domain**）。该数据集原始规模约 10.0k 个文件（228.46 MB），含 `training_set/{cats,dogs}` 与 `test_set/{cats,dogs}` 两级目录；本项目使用的是**其中一个子集**，经清洗整理后按 `ImageFolder` 结构组织为训练集与测试集，实测各类别数量如下：

| 数据集 | cats | dogs | 合计 |
|--------|------|------|------|
| 训练集 `data/train` | 860 | 861 | 1721 |
| 测试集 `data/test` | 152 | 152 | 304 |

> **数据规模说明**：测试集每类仅 152 张（共 304 张），样本量相对有限。因此准确率的绝对数值存在一定统计波动，结论应以两方案的相对差距（约 15 个百分点）为主要依据。此外需说明：本项目只使用了上游数据集的子集（本地合计 2025 张，上游约 10.0k 个文件），且子集的挑选与去重过程没有留下记录，故"数据规模"相关的论断（见 5.5 节）应谨慎对待。

### 2.2 实验环境

- 软件：Windows + Anaconda，环境 `pytorch`（**Python 3.10.19**）；依赖清单见仓库根目录 `requirements.txt`——`torch` 2.9.0+cu130、`torchvision` 0.24.0+cu130、`numpy` 2.2.6、`pandas` 2.3.3、`matplotlib` 3.10.9、`scikit-learn` 1.7.2、`pillow` 12.0.0、`torchsummary` 1.5.1；
- 硬件：本机（该环境下 `torch.cuda.is_available() == True`，训练脚本自动选择 `cuda`；若不可用则回退 CPU）；
- 评估指标：整体准确率、每类 Precision / Recall / F1、混淆矩阵、ROC 曲线、PR 曲线。

### 2.3 数据预处理

- 从零版：使用 `tools/compute_mean_std.py` 统计出的数据集均值/标准差做归一化，**该统计在 `data/` 全量（含 `test`）上完成**（低维统计量，影响很小，但严格来说属于轻微的测试集信息泄漏，见第 5.5 节）；
- 迁移学习版：使用 ImageNet 统计常数归一化（与预训练权重匹配）。

---

## 3 方法

### 3.1 从零手写 GoogLeNet 及其优化演进

 baseline 版本（`197f95c`）手写实现 GoogLeNet，包含 Inception 多尺度模块，采用 Adam 优化器（学习率 2e-3）、Dropout(0.5)，无数据增强。（原版 GoogLeNet 的两个辅助分类器在本实现中未使用，2 类小任务也不需要。）在此基础上进行五轮优化，提交记录如下：

| 阶段 | 提交号 | 核心改动 |
|------|--------|----------|
| 基线 init | `197f95c` | 手写 GoogLeNet（Inception 多尺度模块）、Dropout(0.5)、Adam lr=2e-3、无增强 |
| 优化1 | `c431283` | 数据增强 + AdamW(wd=1e-4) + label smoothing(0.1) + ReduceLROnPlateau + EarlyStopping + 固定随机种子 |
| 优化2 | `f4e1db0` | 移除 Dropout、关闭 ColorJitter、权重衰减 1e-4→1e-5（松绑正则） |
| 优化3 | `30e8fad` | 全网络加入 Batch Normalization、学习率 2e-3→1e-3 |
| 优化4 | `4b6b009` | 引入迁移学习（见 3.2，新建 `model_transfer.py` / `train_transfer.py`） |
| 优化5 | `d851700` | 测试可视化：输出混淆矩阵 + ROC + PR 三子图与分类报告 |

> 代码级改动细节详见《模型演进报告.md》。

### 3.2 迁移学习路线

迁移学习版复用 `torchvision` 提供的 ImageNet 预训练 GoogLeNet 权重，替换最终分类头为 2 类输出。训练策略为**单阶段微调**：构建模型时一次性冻结全部主干、解冻分类头 `fc` 与最后 3 个 Inception 模块，然后用分层学习率（`fc` 1e-3 / 解冻的 features 1e-4）一起训练；训练阶段冻结层保持 `eval()` 以固定其 BatchNorm 统计。（并未实现"先只训分类头、再解冻微调"的两阶段流程。）该路线与从零版的关键差异在于**特征提取器起点**——前者从百万级图像上习得的通用视觉特征出发，而非随机初始化。

---

## 4 实验结果与分析

### 4.1 训练过程

各优化阶段的训练/验证损失与准确率曲线保存于 `result/`，以下逐一对照分析。

**图 1 基线（init）训练曲线**

![基线训练曲线](result/model_control_train.png)

基线版本无数据增强、仅以 Dropout 正则。曲线显示训练损失持续下降而验证指标明显滞后，训练/验证之间存在较大间隙，表明模型已出现**过拟合**——仅靠 Dropout 不足以在小数据上约束容量。

**图 2 优化1（数据增强 + 强正则）训练曲线**

![优化1训练曲线](result/model1_train.png)

引入数据增强与一整套正则（权重衰减、label smoothing）后，训练/验证间隙较基线明显收窄。但**这里不能把"间隙收窄"解读为"泛化能力提升"**：该阶段 train acc 仅 ≈0.59、val acc 仅 ≈0.57，两者都贴近随机水平，间隙小是因为模型还没开始有效学习，而不是因为泛化变好（基线是 train ≈0.95 / val ≈0.72，那才叫"学到了但有间隙"）。更关键的是，本阶段一次性改动了 5 个变量（增强 / AdamW / weight_decay / label smoothing / 早停），因此该结果无法归因于其中任何一个。

**图 3 优化2（松绑正则）训练曲线**

![优化2训练曲线](result/model2_train.png)

优化2 撤销 Dropout 并将权重衰减由 1e-4 降至 1e-5。曲线显示验证指标较优化1 有所回升，但**该阶段同样一次改动了 3 个变量**（Dropout、ColorJitter、weight_decay），"回升"究竟来自哪一项无法判定。事后复核认为，优化1/2 的瓶颈更可能是训练预算不足：`batch_size=128` 配合 1376 张训练图，每 epoch 只有 11 个梯度步，优化1 全程 28 个 epoch 共 308 步、优化2 共 385 步——用几百个梯度步从零训练一个数百万参数的 GoogLeNet，本身就难以收敛。

**图 4 优化3（加入 Batch Normalization）训练曲线**

![优化3训练曲线](result/model3.train.png)

全网络加入 Batch Normalization 后，损失曲线更加平滑、收敛更稳定，学习率得以降至 1e-3。BN 提供的内部协变量偏移校正，是从零版能够稳定训练的重要支撑。

**图 5 优化4（迁移学习）训练曲线**

![优化4训练曲线](result/model4_train.png)

迁移学习曲线与从零版形成鲜明反差：损失快速下降且验证准确率持续处于高位。预训练特征使模型在训练初期即具备判别能力，无需从像素级重新学习。

### 4.2 测试集性能

在测试集（304 张）上的整体与每类指标如下：

| 方案 | 测试准确率 | cats F1 | dogs F1 | 误判张数 |
|------|-----------|---------|---------|----------|
| 从零手写（最终） | 0.8224 | 0.8138 | 0.8302 | 54 |
| 迁移学习 | 0.9704 | 0.9711 | 0.9697 | 9 |

> **指标口径说明**：表中各项指标均由图 6、图 7 中的显式混淆矩阵反算得到，可逐项复算。分类报告（`classification_report`）的行序本身是正确的（按 `classes` 顺序，`cats` 在前），需要注意的是**测试脚本控制台打印的 TN/FP/FN/TP 文字与图不一致**（脚本假定类别 0 为负类，而实际正类 `cats` 的索引就是 0，详见第 5.5 节第 10 条）。

**从零版混淆矩阵与 ROC/PR（图 6）**

![从零版测试可视化](result/model_test.png)

由混淆矩阵（正类 = cats）可见：真实 cats 152 张中 118 张正确、34 张误判为 dogs；真实 dogs 152 张中 132 张正确、20 张误判为 cats。即模型对**猫的召回偏低（118/152 ≈ 0.776）**，是把猫误判为狗导致精度受限的主因；ROC 与 PR 曲线下面积虽高于随机，但尾部仍有明显错分，与 82.24% 的准确率相互印证。

**迁移版混淆矩阵与 ROC/PR（图 7）**

![迁移版测试可视化](result/model_transfer_test.png)

迁移版混淆矩阵仅 9 张误判（**8 张狗误报为猫、1 张猫漏判为狗**：FP = 8、FN = 1），两类召回均高于 0.94，ROC/PR 曲线紧贴左上角，可视化结果与 97.04% 的准确率一致，说明预训练特征在该任务上几乎给出判别性解。

### 4.3 方法对比与成因分析

**为什么迁移学习远优于从零训练**：从零版需自行完成「边缘→纹理→部件→语义」的完整特征学习，1721 张训练图远不足以支撑如此复杂的映射；迁移版直接继承 ImageNet 通用特征，仅需对齐「猫狗判别」这一薄层语义，因而在有限数据下泛化更佳。

**各优化阶段对从零版的影响**：从图 1 到图 2，训练/验证指标同时塌到接近随机水平，说明问题不是过拟合而是"没学起来"，且该阶段一次改了 5 个变量无法归因；图 3 的松绑同样不可归因；图 4 引入全网络 Batch Normalization 是从零版**唯一明确的转折点**（val acc 由 ≈0.68 抬到 ≈0.88，第 0 轮 loss 也从数百量级回落到 ~1.0）。然而即便叠加全部优化，从零版仍停留在约 82%——但这一现象**不足以推出"数据规模是硬约束"**：在每 epoch 仅 11 个梯度步、总共只有几百步的训练预算下，模型远未收敛，"训练预算不足"与"初期训练不稳定"两个候选原因尚未分离（见 5.5 节）。

---

## 5 讨论

### 5.1 数据规模的局限

本实验训练与测试样本均在「千」量级，尤其测试集每类仅 152 张。在此规模下，模型性能对数据分布与划分较为敏感，绝对准确率宜结合置信区间理解；迁移学习带来的约 15 个百分点提升，方向明确、可信度高。

### 5.2 训练策略中的偏差—方差权衡

优化1 一次性叠加增强、权重衰减、label smoothing、Dropout 与优化器变更，模型表现反而变差；优化2 主动松绑后才略有恢复。**必须指出：这两次都是一次改动多个变量，因此无法把结果归因到某一个正则项上**——"正则过强"只是当时的推测，并不能由现有数据支持（事后复核更倾向于是训练预算不足，见 5.5 节）。

另外，把最终采用的「弱权重衰减 + 几何增强 + BN + 无 Dropout」称为"平衡点"也缺乏依据：图 4 显示该配置下 train acc 已冲到 1.0 而 val acc 只有 ≈0.88，**仍是明显的过拟合**，只是恰好卡在了精度与稳定性的折中位置。

### 5.3 实验可复现性

当前训练脚本仅保存验证集最优权重（`best_model.pth` / `best_model_transfer.pth`），未留存逐 epoch 的权重快照与超参日志，导致中途各轮模型无法事后复测。建议后续实验：①每个 epoch 或每 N 轮保存带编号的 checkpoint；②记录完整超参与随机种子；③将测试指标自动落盘为 `json`/`csv`，便于跨实验纵向对比。

**现状补充（2026-09 复核）**：训练脚本里没有全局随机种子（只有数据切分用了 `torch.Generator().manual_seed(42)`），而 `best_model.pth` 已被后续重跑覆盖。因此本报告中从零版的那套测试数值（82.24% / cats F1 0.8138 / dogs F1 0.8302）属于**当次运行的记录，现已不可复现**：用仓库中现存的 `best_model.pth` 在 `data/test` 上重算，得到 82.57%（cats F1 0.8328 / dogs F1 0.8178，弱项类别由"猫"变为"狗"）。详见 5.5 节第 6 条。

### 5.4 可视化对结论的支撑作用

测试阶段的三子图（混淆矩阵 / ROC / PR）将单一准确率转化为「错在何处、整体区分度如何」的可读信息，是定位瓶颈（如本实验从零版猫类召回不足）的关键手段，应在后续实验中常态化。

### 5.5 已知限制

> 本节如实记录当前版本的短板。它们不影响"迁移学习远优于从零训练"这一主结论，但会直接影响绝对数值的可复现性与可信度。与 `README.md` 的「已知限制」保持同一口径。

1. **从零版远未收敛，不是"到达上限"。** `batch_size=128` 配合 1376 张训练图（1721 的 80%）→ 每 epoch 仅 **11 个梯度步**；优化1 全程 28 个 epoch 共 308 步、优化3 共 40 个 epoch 共 440 步。用几百个梯度步从零训练数百万参数的 GoogLeNet，train acc 停在 0.59（优化1）属于"还没学起来"，因此第 6.1 节关于"数据规模上限"的表述已相应收敛为"训练预算不足与初期训练不稳定两个候选原因尚未分离"。
2. **无全局随机种子。** 只有切分用的 `manual_seed(42)`；模型初始化、`DataLoader` 的 shuffle、Dropout 采样均不受控，重跑不会得到同一条曲线。
3. **无 checkpoint / 断点续训。** 只保存最终最优 `state_dict`，无逐 epoch 快照、无 optimizer / scheduler 状态、无 `--resume` 入口。
4. **归一化统计量含测试集。** 从零版的 `mean/std` 在 `data/` 全量（train + test）上统计得到，属于轻微的测试集信息泄漏；严格做法是只用 `data/train` 统计。
5. **早停 / 选模 / 学习率调度以 val accuracy 为信号，而验证集只有 345 张**（`int(0.8 × 1721) = 1376`，`1721 − 1376 = 345`），1 个样本即 0.29% 的准确率分辨率，噪声极大。迁移版上后果直接暴露：`result/model4_train.png` 第 0 轮 val acc ≈ 0.991 就是全程最高，`early_stop_patience=8` 于是在第 8 轮触发，"最优模型"约等于只训练了 1 个 epoch 的模型。改用 val loss 作为信号会稳健得多。
6. **`best_model.pth` 已被后续重跑覆盖。** 本报告从零版的 82.24%（cats F1 0.8138 / dogs F1 0.8302）为当次运行记录；用现存的 `best_model.pth` 重算为 82.57%（TP = 132 / FN = 20 / FP = 33 / TN = 119，cats F1 0.8328 / dogs F1 0.8178，弱项由**猫**变为**狗**）。两者不同源，历史数值无法还原。
7. **训练曲线为窗口截图。** 两个训练脚本的 `matplot_acc_loss()` 只有 `plt.show()`、没有 `savefig`，`result/` 下的 5 张训练曲线均为 matplotlib 窗口截图；且基线图的 loss 面板被第 0 轮 ≈955 的离群点拉爆，实际不可读。
8. **验证集准确率是"逐 epoch 取最大值"。** 文中的 val acc（如 ≈0.88）是 40 个 epoch 中的最大值，存在乐观偏差，不宜与无偏的 test acc 直接比较。
9. **测试集仅 304 张（每类 152 张）。** 绝对准确率存在统计波动，结论应以两方案的相对差距为准。
10. **测试脚本控制台打印的 TN/FP/FN/TP 与图不一致。** `ImageFolder` 的类别按字母序为 `['cats', 'dogs']`，而脚本设定 `pos_class='cats'`（索引 0），此时 `cm_raw.ravel()` 的展开顺序是 `[TP, FN, FP, TN]`，脚本却按 `[TN, FP, FN, TP]` 赋值，因此控制台四行标签是错的；而绘图函数用 `neg_idx`/`pos_idx` 做了正确重排，图是对的。以图为准，控制台文字不可用。
11. **未实现原版 GoogLeNet 的两个辅助分类器。** 2 类小任务不需要 aux 分支，本实现直接从 `AdaptiveAvgPool2d → Flatten → Linear(1024, 2)` 输出，这与部分经典描述不同（原始 GoogLeNet 才含 aux 分类器）。

**后续修复方向（尚未执行）**：补 `set_seed()`；早停 / 选模信号改用 val loss；归一化只用 `data/train` 重算；加 checkpoint 与 `savefig`；把 `batch_size` 降到 32–64、epoch 提到 100+ 重跑从零版以验证第 1 条；以"增强"为唯一变量做一次干净的 A/B；多 seed 报告 mean ± std；补失败案例分析（可视化从零版的 34 张错分猫）。

---

## 6 结论与建议

### 6.1 结论

1. 小规模数据下，迁移学习（97.04%）显著优于从零训练（82.24%），差距约 15 个百分点；
2. 从零版在本次训练预算下**远未收敛**（每 epoch 仅 11 个梯度步），因此本次实验**不足以断定"数据规模是从零路线的精度上限"**；"训练预算不足"与"初期训练不稳定"两个候选原因尚未分离（见 5.5 节第 1 条）；
3. 在从零版的优化过程中，**全网络 Batch Normalization 是唯一明确的转折点**（val acc ≈0.68 → ≈0.88），数据增强与正则强度的作用则因一次改动多个变量而无法归因；

> 本报告已按上述口径修订；早期版本中"数据规模是硬约束""正则过强导致欠拟合"等表述已被收敛（见 5.5 节的完整列表）。

### 6.2 建议

1. **先把训练预算给够**：把 `batch_size` 降到 32–64、epoch 提到 100+，重跑从零版以验证"远未收敛"这一判断——这是整个结论的地基；
2. **保存训练快照**：增加 epoch 级 checkpoint 与超参日志，并补全局随机种子，保障可复现与事后复测；
3. **改早停 / 选模信号**：把 val accuracy 换成 val loss（连续量），避免 345 张验证集上 0.29% 的分辨率造成的选模噪声；
4. **扩充测试集**：将 `data/test` 补足至每类 1000 张，使评估统计更稳健；
5. **指标落盘**：测试脚本自动写出结构化指标文件，支持跨实验对比；
6. **追求更高精度**：在迁移学习基础上尝试更强骨干（ResNet / EfficientNet）或更细致的数据清洗，而非回到从零训练。

---

## 附录

### A. 图表索引

| 图号 | 内容 | 文件 |
|------|------|------|
| 图1 | 基线训练曲线 | `result/model_control_train.png` |
| 图2 | 优化1 训练曲线 | `result/model1_train.png` |
| 图3 | 优化2 训练曲线 | `result/model2_train.png` |
| 图4 | 优化3 训练曲线 | `result/model3.train.png` |
| 图5 | 优化4（迁移）训练曲线 | `result/model4_train.png` |
| 图6 | 从零版测试可视化（混淆矩阵/ROC/PR） | `result/model_test.png` |
| 图7 | 迁移版测试可视化（混淆矩阵/ROC/PR） | `result/model_transfer_test.png` |

### B. 代码与权重索引

- 网络定义：`model.py`（手写 GoogLeNet）、`model_transfer.py`（迁移构建）
- 训练脚本：`model_train.py`、`train_transfer.py`
- 测试脚本：`model_test.py`、`model_test_transfer.py`
- 权重文件：`best_model.pth`（从零最终）、`best_model_transfer.pth`（迁移）
- 代码演进详解：《模型演进报告.md》
- Git 提交（共 7 个）：`197f95c` → `c431283` → `f4e1db0` → `30e8fad` → `4b6b009` → `d851700` → `a38f57f`（后者只新增 `tools/` 与文档，不属于训练路线阶段）
- 已知限制：见第 5.5 节（与 `README.md` 的「已知限制」同一口径）
