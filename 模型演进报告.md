# GoogLeNet 猫狗分类模型演进报告

> 基于项目 Git 提交历史（`git log`）整理。所有代码均来自真实 `git show` / `git diff`，未做任何臆造。
> 目标：从最初的模型基线出发，按时间顺序记录每一次「优化」**改动了哪些代码**、**代码在做什么**，以及**为什么要这样改**（动机与机器学习原理）。
>
> 说明：本文件最初设想**不纳入 Git 版本控制**，但实际上已随提交 `a38f57f`（增加两个小工具和一些文档）被纳入版本库。此处原表述已更正。

---

## 一、演进总览表

| # | Commit | 阶段 | 关键改动 | 主要文件 |
|---|--------|------|----------|----------|
| 1 | `197f95c` | init 基线 | 手写从零 GoogLeNet + 基础训练/验证流程（无数据增强、无正则） | `model.py`、`model_train.py` |
| 2 | `c431283` | 优化1：增加多种数据增强方法 | 数据增强 + AdamW/权重衰减 + 标签平滑 + LR 自动调度 + 早停 | `model_train.py` |
| 3 | `f4e1db0` | 优化2：松绑正则与增强 | 去 Dropout、关 ColorJitter、weight_decay 1e-4 → 1e-5 | `model.py`、`model_train.py` |
| 4 | `30e8fad` | 优化3：全网络加 BatchNorm 并降低学习率 | 每个卷积后加 BN、lr 0.002 → 0.001 | `model.py`、`model_train.py` |
| 5 | `4b6b009` | 优化4：使用 GoogLeNet 迁移学习 | ImageNet 预训练 + 替换分类头 + 冻结/解冻策略（两个新脚本） | `model_transfer.py`、`train_transfer.py` |
| 6 | `d851700` | 优化5：优化测试函数 | 测试脚本输出混淆矩阵 / ROC / PR 三子图与分类报告 | `model_test.py`、`model_test_transfer.py` |

> 另有 `a38f57f`（当前 HEAD）只新增 `tools/` 两个辅助脚本与文档，不涉及训练路线的代码改动，故未列入上表。

> 一条贯穿始终的演进逻辑线：**数据增强 → 松绑正则 → 用 BN 稳定训练 → 迁移学习借力预训练 → 评估可视化**。本质是在「猫狗二分类、数据量小（train 约 1721、test 304 张）」这个约束下逐步调优。需要提醒的是：从零版各阶段都受限于训练预算（`batch_size=128` 配合 1376 张训练图 → 每 epoch 仅 11 个梯度步），**"82% 是数据规模上限"这一早期说法并不成立**，详见第八章末尾与本报告第九章末尾的复核说明。

---

## 二、阶段一：init 基线（`197f95c`）

### 2.1 改动清单
首次提交，搭起一个**从零手写**的经典 GoogLeNet（含 Inception 模块）来做猫狗二分类，并配有标准训练/验证循环。此时没有数据增强、没有额外正则（仅靠 `Dropout(0.5)`），训练配置也最简单。

### 2.2 关键代码（来自 `git show 197f95c:model.py`）

**Inception 模块（四路并行、通道拼接）：**
```python
class Inception(nn.Module):
    def __init__(self, in_channels, c1, c2, c3, c4):
        super(Inception, self).__init__()
        self.ReLU = nn.ReLU()
        # 路线1：1x1 卷积
        self.p1_1 = nn.Conv2d(in_channels, c1, kernel_size=1)
        # 路线2：1x1 卷积 → 3x3 卷积
        self.p2_1 = nn.Conv2d(in_channels, c2[0], kernel_size=1)
        self.p2_2 = nn.Conv2d(c2[0], c2[1], kernel_size=3, padding=1)
        # 路线3：1x1 卷积 → 5x5 卷积
        self.p3_1 = nn.Conv2d(in_channels, c3[0], kernel_size=1)
        self.p3_2 = nn.Conv2d(c3[0], c3[1], kernel_size=5, padding=2)
        # 路线4：3x3 最大池化 → 1x1 卷积
        self.p4_1 = nn.MaxPool2d(kernel_size=3, stride=1, padding=1)
        self.p4_2 = nn.Conv2d(in_channels, c4, kernel_size=1)

    def forward(self, x):
        p1 = self.ReLU(self.p1_1(x))
        p2 = self.ReLU(self.p2_2(self.ReLU(self.p2_1(x))))
        p3 = self.ReLU(self.p3_2(self.ReLU(self.p3_1(x))))
        p4 = self.ReLU(self.p4_2(self.p4_1(x)))
        return torch.cat((p1, p2, p3, p4), 1)  # 在通道维度拼接
```

**GoogLeNet 主体（卷积栈 + Inception 堆叠 + 分类头）：**
```python
class GoogLeNet(nn.Module):
    def __init__(self, Inception):
        super(GoogLeNet, self).__init__()
        self.b1 = nn.Sequential(
            nn.Conv2d(3, 64, 7, stride=2, padding=3), nn.ReLU(),
            nn.MaxPool2d(3, stride=2, padding=1))
        self.b2 = nn.Sequential(
            nn.Conv2d(64, 64, 1, padding=1), nn.ReLU(),
            nn.Conv2d(64, 192, 3, padding=1), nn.ReLU(),
            nn.MaxPool2d(3, stride=2, padding=1))
        self.b3 = nn.Sequential(
            Inception(192, 64, (96, 128), (16, 32), 32),
            Inception(256, 128, (128, 192), (32, 96), 64),
            nn.MaxPool2d(3, stride=2, padding=1))
        self.b4 = nn.Sequential(  # 5 个 Inception
            Inception(480, 192, (96, 208), (16, 48), 64),
            Inception(512, 160, (112, 224), (24, 64), 64),
            Inception(512, 128, (128, 256), (24, 64), 64),
            Inception(512, 112, (128, 288), (32, 64), 64),
            Inception(528, 256, (160, 320), (32, 128), 128),
            nn.MaxPool2d(3, stride=2, padding=1))
        self.b5 = nn.Sequential(
            Inception(832, 256, (160, 320), (32, 128), 128),
            Inception(832, 384, (192, 384), (48, 128), 128),
            nn.AdaptiveAvgPool2d((1, 1)),
            nn.Flatten(),
            nn.Dropout(0.5),
            nn.Linear(1024, 2))   # 输出 2 类：猫 / 狗
        # 权重初始化：Conv 用 kaiming_normal_；Linear 用 normal_(0, 0.01)
```

**训练循环核心（来自 `git show 197f95c:model_train.py`，节选）：**
```python
# 数据：resize → tensor → 归一化，无增强
train_transform = transforms.Compose([transforms.Resize((224, 224)),
                                      transforms.ToTensor(), normalize])
train_data = ImageFolder(ROOT_TRAIN, transform=train_transform)
train_data, val_data = Data.random_split(
    train_data, [round(0.8 * len(train_data)), round(0.2 * len(train_data))])

optimizer = torch.optim.Adam(model.parameters(), lr=0.002)
criterion = nn.CrossEntEntropyLoss()
# 每个 epoch：model.train() 训练，model.eval()+torch.no_grad() 验证
# 记录 loss/acc，并在 val 准确率创新高时保存 best_model_wts
torch.save(best_model_wts, BEST_MODEL_PATH)
```

### 2.3 代码解释
- **Inception 模块**：GoogleNet 的核心创新——同一输入并行走 4 条不同感受野的支路（1×1、1×1+3×3、1×1+5×5、3×3 池化+1×1），再把输出在**通道维**拼接。好处是用多尺度特征取代单一大卷积，参数更少、表达更强。
- **1×1 卷积**：在 3×3 / 5×5 支路前先做 1×1，用来**降维**（减少后续大卷积的输入通道，省计算）。
- **分类头**：全局平均池化把 7×7 特征压成 1×1，Flatten 后 `Dropout(0.5)` 再接 `Linear(1024, 2)`。
- **权重初始化**：卷积用 Kaiming 正态（适配 ReLU），全连接用 `(0, 0.01)` 正态，避免初始梯度爆炸/消失。
- **训练循环**：标准 PyTorch 训练范式——`zero_grad → forward → loss.backward → step`，按 epoch 在训练/验证间切换，并只保存验证集上最优的那一份权重。

### 2.4 为什么这样做（动机 / 原理）
- 基线的目标是**先跑通一个可控、可解释的起点**：用经典架构 + 最朴素的数据/训练设置，得到一条「对照基准线」。
- 只靠 `Dropout(0.5)` 做正则，是因为此时还没引入增强等更花哨的手段，先单一变量观察。
- **一个遗留隐患**：基线的 `random_split` **没有固定随机种子**，而且 train/val **共用同一套变换**（此时基线还没有任何数据增强，训练变换是 `Resize((224,224)) + ToTensor + Normalize`，所以不存在"增强泄漏"）。真正的后果是切分不可复现。下一阶段（优化1）用固定 `seed(42)` 切分 + 验证集纯净变换修正了它。

---

## 三、阶段二：优化1 —— 增加多种数据增强方法（`c431283`）

### 3.1 改动清单
commit message 写的是「增加多种数据增强方法」，但真实 diff 实际上**顺带引入了一整套训练稳定性 / 正则化手段**。核心改动如下（全部在 `model_train.py`）：

### 3.2 关键代码（来自 `git diff 197f95c c431283 -- model_train.py`，真实片段）

**(a) 训练集加增强、验证集保持纯净，并用固定 seed 切分：**
```python
# 训练集：数据增强（增强作用于 PIL 图，放在 ToTensor/Normalize 之前）
train_transform = transforms.Compose([
    transforms.RandomResizedCrop(224, scale=(0.8, 1.0)),
    transforms.RandomHorizontalFlip(),
    transforms.ColorJitter(brightness=0.2, contrast=0.2, saturation=0.2),
    transforms.ToTensor(),
    normalize,
])
# 验证集：纯净变换，不做增强（避免增强泄漏到验证集、扭曲评估分布）
val_transform = transforms.Compose([
    transforms.Resize((224, 224)),
    transforms.ToTensor(),
    normalize,
])
# 同一份固定 seed 切分，保证 train/val 严格不重叠、可复现
full = ImageFolder(ROOT_TRAIN)
train_idx, val_idx = Data.random_split(
    range(len(full)),
    [int(0.8 * len(full)), len(full) - int(0.8 * len(full))],
    generator=torch.Generator().manual_seed(42))
train_ds = ImageFolder(ROOT_TRAIN, transform=train_transform)
val_ds   = ImageFolder(ROOT_TRAIN, transform=val_transform)
train_dataloader = Data.DataLoader(Data.Subset(train_ds, train_idx), batch_size=128, shuffle=True,  num_workers=2)
val_dataloader   = Data.DataLoader(Data.Subset(val_ds,   val_idx), batch_size=128, shuffle=False, num_workers=2)
```

**(b) 优化器 / 损失 / 学习率调度 / 早停：**
```python
# 权重衰减抑制过拟合（AdamW 把权重衰减与梯度解耦）
optimizer = torch.optim.AdamW(model.parameters(), lr=0.002, weight_decay=1e-4)
# 标签平滑：降低模型对训练样本过度自信
criterion = nn.CrossEntropyLoss(label_smoothing=0.1)
# 验证集指标停滞时自动降低学习率，帮助跳出平台
scheduler = torch.optim.lr_scheduler.ReduceLROnPlateau(
    optimizer, mode='max', factor=0.5, patience=5)

# 早停：连续 early_stop_patience 轮验证准确率无提升则停止
early_stop_patience = 10
epochs_no_improve = 0
...
# 每个 epoch 末尾：
scheduler.step(val_acc_all[-1])        # 根据验证准确率调整 LR
if val_acc_all[-1] > best_acc:
    best_acc = val_acc_all[-1]
    best_model_wts = copy.deepcopy(model.state_dict())
    epochs_no_improve = 0
else:
    epochs_no_improve += 1
    if epochs_no_improve >= early_stop_patience:
        print("Early stopping ...")
        break
```

### 3.3 代码解释
- **`RandomResizedCrop(scale=(0.8,1.0))`**：随机裁剪原图的 80%~100% 区域再缩放 —— 让模型对物体位置和尺度更鲁棒。
- **`RandomHorizontalFlip`**：随机水平翻转 —— 猫狗左右翻转不改变类别，低成本增广。
- **`ColorJitter`**：随机扰动亮度/对比度/饱和度 —— 模拟光照变化。
- **train/val 用不同变换 + 固定 seed 切分**：增强只施加在训练集，验证集保持「原图 + resize + 归一化」的纯净分布；`manual_seed(42)` 保证每次切分完全一致、互不重叠、可复现（修正了基线的隐患）。
- **AdamW + `weight_decay=1e-4`**：AdamW 把 L2 权重衰减从梯度更新中解耦出来，正则更干净、更不易干扰 Adam 的自适应动量。
- **`label_smoothing=0.1`**：训练目标从「硬标签 0/1」变成「0.05/0.95」这类软化标签，抑制模型对训练样本过度自信、缓解过拟合。
- **`ReduceLROnPlateau`**：验证准确率不再上升时，把学习率乘 0.5，帮优化跳出平坦区。
- **Early Stopping**：连续 10 轮验证不涨就停，省时间也防后期过拟合。

### 3.4 为什么这样做（动机 / 原理）
- **数据增强是「小数据集」的头号武器**：本项目训练图仅约 1700 张，模型容量（GoogLeNet 数百万参数）远大于数据量，**极易过拟合**。增强在「不增加真实样本」的前提下，大幅扩充了输入分布的多样性，等价于免费多了很多「新图」。
- **增强只用于训练、不用于验证**：否则验证集分布被扭曲，评估指标失真（「增强泄漏」）。
- **引入 AdamW / label smoothing / 早停**：在增强之外再叠一层正则与安全网——增强本身已是强正则，这里配合权重衰减与标签平滑进一步压过拟合，早停与 LR 调度则提升训练效率与稳定性。
- 一句话：**这一阶段的主题是「用增强 + 正则 + 训练技巧，把从小数据里学到的东西泛化得更好」**。

---

## 四、阶段三：优化2 —— 松绑正则与增强（`f4e1db0`）

### 4.1 改动清单
commit message 写「松绑正则与增强」。真实 diff 只改了 3 行（`model.py` 1 行、`model_train.py` 2 行）：去掉 `Dropout(0.5)`、把 `ColorJitter` 注释关闭、`weight_decay` 从 `1e-4` 降到 `1e-5`。

### 4.2 关键代码（`git diff c431283 f4e1db0`）

**model.py —— 移除 Dropout：**
```diff
         nn.AdaptiveAvgPool2d((1, 1)),
         nn.Flatten(),
-        nn.Dropout(0.5),
         nn.Linear(1024, 2),
```

**model_train.py —— 关闭 ColorJitter、降低 weight_decay：**
```diff
         transforms.RandomHorizontalFlip(),
-        transforms.ColorJitter(brightness=0.2, contrast=0.2, saturation=0.2),
+        # transforms.ColorJitter(...)  # 第一轮：暂时关闭，观察是否为欠拟合元凶
         transforms.ToTensor(),
-    optimizer = torch.optim.AdamW(model.parameters(), lr=0.002, weight_decay=1e-4)
+    optimizer = torch.optim.AdamW(model.parameters(), lr=0.002, weight_decay=1e-5)  # weight_decay 1e-4→1e-5
```

### 4.3 代码解释
- **`Dropout(0.5)` 被删**：分类头前的随机置零正则被取消。
- **`ColorJitter` 被注释**：训练增强中最「激进」的色彩扰动被关掉（代码注释说明是「观察是否为欠拟合元凶」）。
- **`weight_decay` 1e-4 → 1e-5**：权重衰减强度降到原来的 1/10。

### 4.4 为什么这样做（动机 / 原理）
- 这是一次**反向「松绑」**：优化1 一口气加了增强 + Dropout + 权重衰减 + 标签平滑 + 优化器变更，当时的判断是正则力可能**过强**，把模型压到欠拟合（train acc 也上不去）。
  > **复核更正（2026-09）**：这是当时的推断，并非已证实的结论。优化1 一次改动了 5 个变量、优化2 又同时改了 3 个，因此"正则过强"无法被归因；而每 epoch 仅 11 个梯度步（优化1 全程 28 个 epoch 共 308 步）意味着"训练预算不足"同样是强候选解释，二者尚未分离。
- 于是退一步：**保留基础的几何增强（裁剪、翻转）和标签平滑，但撤掉色彩扰动、Dropout，并大幅减弱权重衰减**，给模型「松绑」、恢复容量。
- 核心思路：**正则要够，但不能压垮学习能力**——用消融式的小步调整（关一个、降一个）来定位到底是哪一味药太猛。这也是实践中调参的常见手法。

---

## 五、阶段四：优化3 —— 全网络加 BatchNorm 并降低学习率（`30e8fad`）

### 5.1 改动清单
在 `model.py` 的 Inception 每个卷积后、以及 `b1`/`b2` 卷积后都插入 `BatchNorm2d`；并把训练学习率从 `0.002` 降到 `0.001`。

### 5.2 关键代码（`git diff f4e1db0 30e8fad`）

**Inception 里每条支路卷积后加 BN（Conv→BN→ReLU）：**
```diff
         self.p1_1 = nn.Conv2d(in_channels, c1, kernel_size=1)
+        self.p1_bn = nn.BatchNorm2d(c1)
         self.p2_1 = nn.Conv2d(in_channels, c2[0], kernel_size=1)
+        self.p2_1_bn = nn.BatchNorm2d(c2[0])
         self.p2_2 = nn.Conv2d(c2[0], c2[1], kernel_size=3, padding=1)
+        self.p2_2_bn = nn.BatchNorm2d(c2[1])
         # 路线3、路线4 同理各加对应 BN ...
     def forward(self, x):
-        p1 = self.ReLU(self.p1_1(x))
-        p2 = self.ReLU(self.p2_2(self.ReLU(self.p2_1(x))))
+        p1 = self.ReLU(self.p1_bn(self.p1_1(x)))
+        p2 = self.ReLU(self.p2_2_bn(self.p2_2(self.ReLU(self.p2_1_bn(self.p2_1(x))))))
         ...
```
**b1 / b2 也加 BN：**
```diff
     self.b1 = nn.Sequential(
         nn.Conv2d(3, 64, 7, stride=2, padding=3),
+        nn.BatchNorm2d(64),
         nn.ReLU(),
         nn.MaxPool2d(3, stride=2, padding=1))
     self.b2 = nn.Sequential(
         nn.Conv2d(64, 64, 1, padding=1),
+        nn.BatchNorm2d(64),
         nn.ReLU(),
         nn.Conv2d(64, 192, 3, padding=1),
+        nn.BatchNorm2d(192),
         nn.ReLU(),
         nn.MaxPool2d(3, stride=2, padding=1))
```
**学习率下调：**
```diff
-    optimizer = torch.optim.AdamW(model.parameters(), lr=0.002, weight_decay=1e-5)
+    optimizer = torch.optim.AdamW(model.parameters(), lr=0.001, weight_decay=1e-5)  # 加 BN 后 lr 降到 0.001 更稳
```

### 5.3 代码解释
- **BatchNorm2d**：对每个通道在 batch 维度上做标准化（减均值、除标准差，再学缩放/平移参数 γ、β），使每一层输入分布保持「均值 0、方差 1」附近。
- 顺序采用经典 **Conv → BN → ReLU**（部分支路 1×1 后先 BN 再 ReLU，再进下一卷积）。
- lr 从 0.002 降到 0.001，是加了 BN 之后的配套调整。

### 5.4 为什么这样做（动机 / 原理）
- **BN 解决「内部协变量偏移」**：深层网络每层输入分布随前层参数更新而漂移，BN 把每层输入拉回稳定分布，让训练更稳、梯度更健康。
- **BN 通常允许更大的初始学习率**；但这里反而**下调** lr，是因为 BN 改变了梯度尺度，先以更保守的步长跑，避免引入 BN 后初期抖动。
- 配合优化2 的「松绑」，此时网络变成「**有 BN + 轻正则 + 几何增强**」的较健康配置，为后续进一步提效打底。

---

## 六、阶段五：优化4 —— 使用 GoogLeNet 迁移学习（`4b6b009`）

### 6.1 改动清单
这是一次**范式升级**：不再从零训练，而是加载 ImageNet 预训练的 GoogLeNet，把分类头换成 2 类，再**冻结大部分、解冻少量高层**做微调。新增两个文件 `model_transfer.py`、`train_transfer.py`。

### 6.2 关键代码（来自 `git show 4b6b009:model_transfer.py`）

**构建迁移模型（预训练 + 替换头 + 冻结/解冻）：**
```python
def build_transfer_model(num_classes=2, unfreeze_last_n=3):
    # 加载 ImageNet 预训练权重
    model = models.googlenet(weights=GoogLeNet_Weights.IMAGENET1K_V1)
    # 替换分类头（原 1000 类，直接丢弃）
    model.fc = nn.Linear(model.fc.in_features, num_classes)

    # 1) 先冻结全部参数
    for p in model.parameters():
        p.requires_grad = False
    # 2) 解冻分类头
    for p in model.fc.parameters():
        p.requires_grad = True
    # 3) 解冻最后 N 个 Inception block（精确匹配类型，避免误算无参数的 MaxPool）
    inceptions = [m for _, m in model.named_children()
                  if type(m).__name__ == 'Inception']
    for blk in inceptions[-unfreeze_last_n:]:
        for p in blk.parameters():
            p.requires_grad = True
    # 重新初始化分类头权重（避免沿用 1000 类的尺度）
    nn.init.normal_(model.fc.weight, 0, 0.01)
    nn.init.constant_(model.fc.bias, 0)
    return model
```

**训练脚本关键点（来自 `git show 4b6b009:train_transfer.py`，节选）：**
```python
# 迁移学习必须用 ImageNet 归一化常数（预训练权重基于此分布训练）
normalize = transforms.Normalize(mean=[0.485, 0.456, 0.406],
                                 std=[0.229, 0.224, 0.225])

# 分层学习率：分类头 fc 用大 lr，解冻的 features 用小 lr（保护预训练特征）
fc_params, feat_params = [], []
for name, p in model.named_parameters():
    if not p.requires_grad:
        continue
    (fc_params if name.startswith('fc') else feat_params).append(p)
optimizer = AdamW([
    {'params': fc_params,  'lr': 1e-3},
    {'params': feat_params, 'lr': 1e-4},
], weight_decay=1e-4)

# 冻结层保持 eval（BN 用预训练 running stats，不被新数据污染），
# 仅解冻的 fc + dropout + 最后 3 个 Inception block 进入 train 模式
model.eval()
model.fc.train()
model.dropout.train()
unfrozen = [m for _, m in model.named_children()
            if type(m).__name__ == 'Inception'][-3:]
for blk in unfrozen:
    blk.train()
```

### 6.3 代码解释
- **`models.googlenet(weights=IMAGENET1K_V1)`**：直接拿到在百万级 ImageNet 上训好的网络，底层卷积已学会「边缘 / 纹理 / 部件」等通用视觉特征。
- **替换 `fc`**：把 1000 类分类头换成 `Linear(in_features, 2)`。
- **冻结/解冻策略**：默认全冻结，只让分类头 + 最后 3 个 Inception 可学习——**底层通用特征不动，只重训高层语义与新任务头**，既快又不易过拟合。
- **分层学习率**：分类头用 `1e-3`（大一点快速适配），解冻的 features 用 `1e-4`（小一点保护预训练权重）。
- **冻结层保持 `eval`**：被冻结的 BN 仍用 ImageNet 上统计的 running stats，避免小数据集把它们「污染」；只有解冻的几层走 `train()`。
- **ImageNet 归一化**：预训练权重的输入分布是 ImageNet 的均值/方差，必须匹配，否则「输入分布错配」会直接崩精度（这也是为什么不能用手写版那套 `[0.481,0.447,0.407]`）。

### 6.4 为什么这样做（动机 / 原理）
- **小数据从零训练的天花板太低**：猫狗只有约 1700 张训练图，从随机初始化训 GoogLeNet 很难训好（前面 init~优化3 一直在和过拟合/欠拟合搏斗）。
- **迁移学习「站在巨人的肩膀上」**：ImageNet 预训练特征对自然图像高度通用，迁移到猫狗只需微调高层即可，往往**数据少、训练快、精度高**。
- 结果也印证了这一点：迁移版测试准确率约 **0.9704**，远高于手写从零版的 **0.8224**。
- 这是本项目演进中**收益最大的一步**——从「调参救小数据」升级到「用预训练范式」。

---

## 七、阶段六：优化5 —— 优化测试函数（测试可视化）（`d851700`）

### 7.1 改动清单
新增 / 重写 `model_test.py` 与 `model_test_transfer.py`，让测试阶段不再只打印一个准确率，而是输出**混淆矩阵 + ROC 曲线 + PR 曲线**三子图组合，并打印分类报告（Precision / Recall / F1 / Support）。

> 注：该 commit 引入的版本使用了一个 `TRANSFER_SHOW` 环境变量开关来控制是否弹窗；在后续的对话维护中，已按你的要求**简化为去掉开关、默认直接 `plt.show()` 弹窗**（仅此一处改动，绘图逻辑未变）。

### 7.2 关键代码（来自当前 `model_test.py`，即 `d851700` 的可视化成果）

**测试时收集真实/预测/正类概率（节选）：**
```python
output = model(test_data_x)                     # logits
probs = torch.softmax(output, dim=1)
pre_lab = torch.argmax(output, dim=1)
y_true.extend(test_data_y.cpu().numpy().tolist())
y_pred.extend(pre_lab.cpu().numpy().tolist())
y_score.extend(probs[:, pos_idx].cpu().numpy().tolist())   # 正类（cats）预测概率，用于 ROC/PR
```

**三子图组合图（节选核心）：**
```python
fig, axes = plt.subplots(1, 3, figsize=(18, 6))
# 子图1：二分类混淆矩阵（TN/FP/FN/TP 标注）
# 子图2：ROC 曲线（fpr, tpr, auc）
fpr, tpr, _ = roc_curve(y_true, y_score, pos_label=pos_idx)
roc_auc = auc(fpr, tpr)
# 子图3：PR 曲线（precision, recall, average_precision）
precision, recall, _ = precision_recall_curve(y_true, y_score, pos_label=pos_idx)
ap = average_precision_score(y_true, y_score, pos_label=pos_idx)
```

### 7.3 代码解释
- **混淆矩阵**：把预测按「真实×预测」二维计数，直接看出模型**主要错在哪一类**。本项目从零版（正类 = cats）的错分方向是**猫更容易被误判为狗**：FN = 34、FP = 20，即 34 张猫被判成狗、20 张狗被判成猫，因此瓶颈是猫的召回（118/152 ≈ 0.776）。
- **ROC / AUC**：用正类预测概率画「假阳率 vs 真阳率」，AUC 衡量**排序质量**（不依赖阈值），越接近 1 越好。
- **PR 曲线 / AP**：关注正类的查准率-查全率权衡，对**类别不平衡**更敏感，比准确率更诚实。
- 这些指标都比单一「整体准确率」信息量大，尤其在小测试集（304 张）上，单独看准确率容易掩盖类别偏斜。

### 7.4 为什么这样做（动机 / 原理）
- 「准确率高」不等于「模型好」：混淆矩阵能暴露偏向、ROC/PR 能评估阈值无关的排序能力。
- 对**二分类 + 小测试集**场景，可视化评估是标准做法——既便于自己诊断，也便于对外展示与对比（例如后续想对比手写版 vs 迁移版）。

---

## 八、总结：一条演进逻辑线

把六个阶段串起来，是一条清晰且合理的路线：

1. **init 基线**：从零搭好经典 GoogLeNet，建立可控对照（无增强、弱正则）。
2. **优化1**：面对小数据过拟合，引入**数据增强 + 一整套正则/训练技巧**（AdamW、标签平滑、LR 调度、早停）。
3. **优化2**：判断正则可能过强，**松绑**（撤 Dropout / 色彩扰动、减弱权重衰减）。注意这一步与优化1 一样属于"多变量同时改动"，结论不可归因。
4. **优化3**：引入 **BatchNorm** 稳定训练分布，并下调 lr 配合。这是从零版**唯一明确的转折点**（val acc ≈0.68 → ≈0.88）。
5. **优化4**：在从零版调了三轮仍停在 82% 附近之后，直接**迁移学习**——用 ImageNet 预训练特征，精度从 0.82 跃升到 0.97，收益最大。
6. **优化5**：补上**评估可视化**，用混淆矩阵/ROC/PR 全面量化模型表现。

整体就是从「**在小数据上死磕从零模型**」逐步转向「**借预训练范式 + 规范评估**」的过程，每一步都是针对上一阶段暴露的问题所做的针对性改进。

**复核说明（2026-09）**：以上逻辑线是对"当时怎么想、怎么改"的忠实记录，但其中两处结论需要收敛：

- **"小数据从零训练天花板低 / 数据规模是硬约束"缺乏直接证据。** 从零版每 epoch 仅 11 个梯度步（优化1 共 28 个 epoch = 308 步、优化3 共 40 个 epoch = 440 步），在这个预算下模型远未收敛，因此 82% 究竟是"训练不足的产物"还是"数据规模上限"，目前无法判定——"训练预算不足"与"初期训练不稳定（第 0 轮 loss 曾达 ~955 / ~185 / ~330）"两个候选原因尚未分离。
- **"正则过强导致欠拟合"未被证实。** 优化1 一次改动 5 个变量、优化2 一次改动 3 个变量，均无法归因；而且基线 / 优化1 / 优化2 的 loss 面板分别被第 0 轮的离群点压平，连"看训练损失是否还在下降"这个判据都用不上。

---

## 九、附：关于「能否靠 Git 回滚历史模型」的说明（与演进相关）

由于本项目的 `.gitignore` 明确排除了 `*.pth` 与 `data/`，**模型权重从未进入版本控制**，因此：

- Git 仓库里保存的是「代码 + 训练曲线图（result/*.png）」，**没有任何历史 `.pth` 权重**；
- 想复现某个优化阶段的模型，**只能 `git checkout` 到对应 commit 的代码 + 用本地仍在的 `data/` 重新训练**，得到的是「同配置重训的新模型」而非当年那份（权重因随机初始化不可逐位复现）；
- 训出的新权重会写入同一个 `best_model.pth`，**训练前务必先备份当前权重**。

**而这件事已经发生了**：仓库中现存的 `best_model.pth` 已被后续重跑覆盖，用它在 `data/test` 上重算得到 **82.57%**（TP = 132 / FN = 20 / FP = 33 / TN = 119，cats F1 0.8328 / dogs F1 0.8178，弱项为**狗**），与 `result/model_test.png` 及文档中那次运行的 **82.24%**（cats F1 0.8138 / dogs F1 0.8302，弱项为**猫**）**不再同源**，历史数值已无法还原。这也是"无随机种子 + 无 checkpoint"的直接后果——相关记录见 `README.md` 与《完整实验报告.md》的「已知限制」。

---

*本报告所有代码片段均提取自项目 Git 历史（`git show` / `git diff`），可对照仓库验证。报告文件最初设想不纳入版本控制，现已随 `a38f57f` 提交。*
